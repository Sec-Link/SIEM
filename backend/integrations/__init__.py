# integrations app package

# -----------------------------
# 中文注释：
# 本模块为 Django app 包装文件（package marker）。
# 主要用途是让 Python 将 `integrations` 视作一个包，并可在此处放置包级别的初始化逻辑或导出符号。
# 当前项目仅使用该文件作为占位符，因此不包含运行时逻辑。
# -----------------------------
